--- 
-  hosts: all 
   gather_facts: no
   tasks:
      - name: Check state
        command: chdir=pollicino ./state.sh
        register: output

      - debug: 
         var: output.stdout_lines
      - debug:
         var: output.stderr_lines